[project]
name = "claim-cloud-id"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "meraki>=2.2.0",
    "python-dotenv>=1.1.0",
]
